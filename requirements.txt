# Runtime extras. Refuel still works without them; only the tray icon and toasts go away.
pystray>=0.19
pillow>=10.0
winotify>=1.1.0
qrcode>=7.0
cryptography>=42.0

# For building: pip install pyinstaller
