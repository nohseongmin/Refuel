# Runtime extras. Refuel still works without them; you lose the tray icon, native
# toasts, the pairing QR image (the link still works), and encrypted phone-sync status.
pystray>=0.19
pillow>=10.0
winotify>=1.1.0
qrcode>=7.0
cryptography>=42.0

# For building: pip install pyinstaller
